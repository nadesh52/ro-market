import React from "react";

const FilterButton = () => {
  return (
      <button className="absolute active right-3 top-1/2 -translate-y-1/2 w-fit rounded-lg bg-zinc-800 px-2 py-1 text-lg font-medium">
        Filter
      </button>
  );
};

export default FilterButton;
